{
  "name": "socket-ts",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:shadel/socket-ts.git",
  "author": "trieu.vo <trieu.vo@insightus.com.au>",
  "license": "MIT",
  "devDependencies": {
    "husky": "^7.0.0"
  },
  "scripts": {
    "prepare": "husky install",
    "test": "husky -v",
    "client": "cd client && yarn start",
    "server": "cd server && yarn start",
    "preinstall": "cd client && yarn install && cd ../server && yarn install"
  }
}
